# LLM endpoint (required) — any OpenAI-compatible /v1/chat/completions server
TEACHER_BASE_URL=http://<host>/v1

# Model name served at the endpoint above (required)
TEACHER_MODEL=<model-name>

# API key for the endpoint above (required)
TEACHER_API_KEY=<api-key>
